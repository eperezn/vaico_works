version: '2'
services:
  nginx:
    container_name: nginx-server
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - vaico
  vaico:
    container_name: vaico
    restart: always
    build: .
    ports:
     - "5000:5000"
